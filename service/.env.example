# Required: pre-shared key matching the browser extension
PSK=your-random-psk-here

# Path where the Netscape cookie file will be written
COOKIE_FILE=/data/cookies.txt

# Server bind address and port
HOST=0.0.0.0
PORT=8080
